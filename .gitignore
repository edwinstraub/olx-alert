# Python
__pycache__/
*.py[cod]
.pytest_cache/

# Virtual environments
.venv/
venv/
.venv.stale.*/

# Local environment / secrets — never commit credentials
.env
github-access-token.txt
*token*.txt
*.pem

# NOTE: state/seen.json is intentionally NOT ignored. It is committed by the
# scheduled workflow so seen-listing state persists across runs.
